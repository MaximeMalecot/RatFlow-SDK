{
    "name": "root",
    "private": true,
    "workspaces": [
        "packages/**",
        "examples/**"
    ],
    "devDependencies": {
        "lerna": "^6.6.2"
    },
    "scripts": {
        "build": "npx lerna run build"
    }
}
